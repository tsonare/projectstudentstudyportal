# def create_note(self, user_id=2, title="Unit test case", description="Testing Unit test case"):
#     return Note.objects.create(user_id=user_id, title=title, description=description)

# def test_note_creation(self):
#     note = self.create_note()
#     self.assertTrue(isinstance(note, Note))
#     self.assertEqual(note.__str__(), note.title)
